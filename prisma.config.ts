// Prisma config — DISABLED until Prisma 6 is installed (currently on 5.20).
// The `prisma/config` import and `defineConfig` API are Prisma-6-only.
// Migration/seed commands can still run via `npx prisma migrate` / `ts-node` directly.
// Re-enable by upgrading to Prisma 6 and restoring the config below.
export {};